@{
    ViewData["Title"] = "Clientes";
    Layout = "~/Views/Shared/_MaterializeExactLayout.cshtml";
}

<div class="container-xxl flex-grow-1 container-p-y">
    <!-- Breadcrumbs -->
    <nav class="breadcrumb-nav mb-4">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="/Admin">Inicio</a>
            </li>
            <li class="breadcrumb-item active">Clientes</li>
        </ol>
    </nav>

    <!-- Page header -->
    <div class="card mb-4">
        <div class="card-body">
            <h4 class="card-title mb-2">Gestión de Clientes</h4>
            <p class="text-muted mb-0">Administra los clientes del hotel</p>
        </div>
    </div>

    <!-- Content placeholder -->
    <div class="card">
        <div class="card-body text-center py-5">
            <i class="fas fa-user-friends fa-3x text-muted mb-3"></i>
            <h5 class="text-muted">Módulo de Clientes</h5>
            <p class="text-muted">Esta funcionalidad estará disponible próximamente</p>
        </div>
    </div>
</div>